import { SortArrayNumberPipe } from './sort-array-number.pipe';

describe('SortArrayNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SortArrayNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
